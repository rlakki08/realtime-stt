# ElevenLabs Scribe v2 Transcription Tool
# Install with: pip install -r requirements.txt

# Core dependencies
elevenlabs>=1.0.0
python-dotenv>=1.0.0

# Real-time transcription dependencies
websockets>=12.0
pyaudio>=0.2.14

# Keyboard control dependencies
pynput>=1.7.6

# Note: pyaudio requires system-level audio libraries:
# - macOS: brew install portaudio
# - Ubuntu/Debian: sudo apt-get install portaudio19-dev python3-pyaudio
# - Windows: pip install pyaudio (usually works directly)
